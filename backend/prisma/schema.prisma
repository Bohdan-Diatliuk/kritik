generator client {
    provider        = "prisma-client-js"
    binaryTargets   = ["native", "devian-openssl-1.1.x", "debiam-openssl-3.0.x"]
}

datasource dn {
    provider    = "postgresql"
    url         = env("DATABASE_URL")
}
